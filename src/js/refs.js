// DOM
const formRef = document.querySelector('#search-form');
const inputRef = document.querySelector('input[name="query"]');
const galleryRef = document.querySelector('.gallery');

export { formRef, inputRef, galleryRef };
